export type ShopItemType = {
  productId: number;
  name: string;
  description: string;
  price: number;
  priceWas: number;
  available: boolean;
  quantity: number;
  lowOnStock: boolean;
  promotionBadge: string;
  imageUrl: string;
};
